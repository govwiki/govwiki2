{% extends "GovWikiAdminBundle:Layout:wizard.html.twig" %}

{% block panel_title %}
    Map
{% endblock panel_title %}

{% block breadcrumb %}
    <li><a href="{{ path('step1') }}">Setup environment</a></li>
    <li class="active">Setup map</li>
{% endblock breadcrumb %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="btn-group pull-left">
            <button class="btn btn-primary" type="submit">Next</button>
            <a class="btn btn-default" href="{{ back_url }}">Back</a>
        </div>
        <a class="pull-right btn btn-danger" href="{{ path('govwiki_admin_wizard_cancel') }}">Cancel</a>
    {{ form_end(form) }}
{% endblock content %}
