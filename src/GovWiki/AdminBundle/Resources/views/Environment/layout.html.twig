{% extends "GovWikiAdminBundle:Layout:environment.html.twig" %}

{% block submenu %}
    {{- macro.submenuElement(
        'Info',
        'govwiki_admin_environment_show',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Templates',
        'govwiki_admin_environment_template',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Localizations',
        'govwiki_admin_localization_index',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Styles',
        'govwiki_admin_style_index',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Map',
        'govwiki_admin_environment_map',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Legend',
        'govwiki_admin_legend_edit',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Format',
        'govwiki_admin_environment_format',
        { 'environment': environment.slug }
    )-}}

    {{- macro.submenuElement(
        'Users',
        'govwiki_admin_user_index',
        { 'environment': environment.slug }
    )-}}
{% endblock submenu %}