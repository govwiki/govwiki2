{% extends '@GovWikiAdmin/Layout/main.html.twig' %}

{% block title %}New oAuth clients{% endblock %}

{% block body %}
    <div class="container-fluid">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">Create</button>
        <a class="btn btn-default" href="{{ path('client_list') }}">Back</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
