<div class="row header">

    {# Logo #}
    <div class="col-xs-6 col-sm-6 col-md-3">

        <a href="http://californiapolicycenter.org/">
            {% if government is defined and government.secondaryLogoUrl %}
                <img src="" alt="California Policy Center" class="logo" width="70%">
                <img src="{{ asset(government.secondaryLogoUrl) }}" width="25%">
            {% elseif elected.government is defined and elected.government.secondaryLogoUrl %}
                <img src="" alt="California Policy Center" class="logo" width="70%">
                <img src="{{ asset(elected.government.secondaryLogoUrl) }}" width="25%">
            {% else %}
                <img src="" alt="California Policy Center" class="logo">
            {% endif %}
        </a>

    </div>

    {# Menu button (hamburger) for mobile devices #}
    <div class="col-xs-6 col-sm-6 visible-xs visible-sm">

        <button type="button" class="hamburger navbar-toggle collapsed" data-toggle="collapse"
                data-target="#bs-navigation-collapse" aria-expanded="false">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>

    </div>

    {# Navigation #}
    <nav class="col-xs-12 col-sm-12 col-md-9 text-right">

        <ul class="navigation navbar-collapse collapse" id="bs-navigation-collapse">

            {% if app.user %}
                {# Show User name (Athorized) #}
                <li>
                    <a href="javascript:void(0)" class="link">
                        Logged in as {{ app.user.username }}
                    </a>
                </li>

                {# Logout button #}
                <li>
                    <a href="{{path('fos_user_security_logout')}}" class="link">
                        Sign Out
                    </a>
                </li>

            {% else %}
                {# Login button#}
                <li>
                    <a href="#" class="link" data-toggle="modal" data-target="#modal-window">
                        Login / Sign Up
                    </a>
                </li>

            {% endif %}

            {# Return to map #}
            {% if app.request.attributes.get('_route') != "map" %}
                <li>
                    <a href="{{ govwiki_path('map') }}" class="link">
                        Return to Map
                    </a>
                </li>
            {% endif %}

            {# Return to some government #}
            {% if app.request.attributes.get('_route') == "elected" %}
                <li>
                    <a href="{{ govwiki_path('government', {
                        altTypeSlug: altTypeSlug,
                        slug: slug
                    }) }}" class="link">
                        Return to {{ slug|replace('_', ' ') }}
                    </a>
                </li>
            {% endif %}

        </ul>

    </nav>

</div>