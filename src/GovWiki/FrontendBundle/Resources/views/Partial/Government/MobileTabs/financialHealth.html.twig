{% if tabName == 'Financial Health' %}
    <div class="col-sm-4">
        {% if government.altTypeSlug != 'School_District' %}
            <div class="customGraph" id="mobile-public-safety-pie"></div>
        {% endif %}
        <div class="customGraph" id="mobile-fin-health-revenue-graph"></div>
        <div class="customGraph" id="mobile-fin-health-expenditures-graph"></div>
    </div>
{% endif %}