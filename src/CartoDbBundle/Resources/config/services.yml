services:
    #
    # Wrapper under curl for easy access to CartoDB api.
    #
    carto_db.api:
        class: CartoDbBundle\Service\CartoDbApi
        arguments:
            - %carto_db.api_key%
            - %carto_db.account%